import styled from 'styled-components'

export const ImagemHeader = styled.div`
  height: 384px;
  width: 100%;
  overflow: hidden;

  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    top: -24px;
  }
`
